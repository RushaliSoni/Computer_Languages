<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rushali.computerlanguages">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/computer3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/computer3"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".forgotpass_activity"></activity>
        <activity android:name=".register_activity" />
        <activity android:name=".login_activity" />
        <activity android:name=".language_detail_activity" />
        <activity android:name=".language_list_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>